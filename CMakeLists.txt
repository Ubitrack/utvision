IF(HAVE_TBB AND HAVE_OPENCV)
	set(the_description "The UbiTrack Vision Module")
	ut_add_module(utvision utcore utdataflow)
	ut_module_include_directories(${UBITRACK_CORE_DEPS_INCLUDE_DIR} ${OPENCV_INCLUDE_DIR} ${TBB_INCLUDE_DIR})
	ut_glob_module_sources(HEADERS "src/*.h" "src/*/*.h" SOURCES "src/*/*.cpp")
	ut_create_module(${TINYXML_LIBRARIES} ${LOG4CPP_LIBRARIES} ${LAPACK_LIBRARIES} ${Boost_LIBRARIES} ${OPENCV_LIBRARIES} ${OpenCL_LIBRARIES} ${TBB_ALL_LIBRARIES})
ENDIF(HAVE_TBB AND HAVE_OPENCV)
